export function About(){return <section className="staticPage"><span className="kicker">ABOUT</span><h1>A shared view of how builders use DSH.</h1><p>DSH Community is an optional, public layer over the local <code>dsh-usage</code> plugin. It ranks absolute aggregate snapshots, so retries replace the same device/day totals instead of double-counting them.</p><div className="principles"><article><strong>Local first</strong><span>Usage and cost stay useful when the Community is offline.</span></article><article><strong>Explicit opt-in</strong><span>GitHub connection and uploading are separate choices.</span></article><article><strong>Aggregate only</strong><span>No prompts, responses, paths, hostnames, or session content.</span></article></div></section>}
export function Privacy(){return <section className="staticPage"><span className="kicker">PRIVACY · V1</span><h1>Small data surface, plainly stated.</h1><p>The Community receives daily and model aggregate token buckets, request counts, a random installation identifier, protocol/plugin versions, and your public GitHub identity. Unknown provider/model routes are collapsed to <code>other</code> before upload. Cost is never uploaded.</p><h2>What remains local</h2><p>Prompts, responses, tool calls, session titles, workspace paths, usernames, hostnames, device hardware identifiers, and raw private model routes.</p><h2>Retention and control</h2><p>Turning Sync off stops future uploads but does not remove accepted Community aggregates. V1 does not yet include self-service deletion. Until it does, request manual removal through the repository's security/contact channel.</p><h2>Public visibility</h2><p>Your profile becomes public after the first successful non-zero sync. Rankings are self-reported and should be read as community signals, not audited billing records.</p></section>}
